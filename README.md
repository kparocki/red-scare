#### To reproduce our results:

1.  Install the dependencies in `requirements.txt`


2.  Run `results.py` in the scripts directory in a UNIX shell.


3.  Results will be returned in `graph_results.csv`

